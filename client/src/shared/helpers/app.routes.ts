enum APP_ROUTES {
  home = "/",
  login = "/login",
  register = "/register",
  posts = "/posts",
  myPosts = "/my-posts",
  post = "/posts/:id",
  createPost = "posts/create-post",
  editPost = "/posts/edit-post/:id",
}

export default APP_ROUTES;
