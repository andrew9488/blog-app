enum APP_ROUTES {
  home = "/",
  login = "/login",
  register = "/register",
  posts = "/posts",
  post = "/posts/:id",
  createPost = "posts/create-post",
  editPost = "/posts/:id/edit-post",
}

export default APP_ROUTES;
